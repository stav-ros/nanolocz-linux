# NL-03 handoff — Typed core contracts

Date: 2026-08-28
Card: NL-03
State: done
Commit: `TODO` (after committing fixes)

## Completed

- Implemented all typed core contracts in `nanolocz/core/types.py`:
  - `Meta`, `Frame`, `Localizations`, `ParticleStack` dataclasses with validation
  - `DetectionParams`, `DetectionResult` for detection workflow
  - `LocalizationParams`, `LocalizedParticle` for localization workflow  
  - `TrackParams`, `ParticleTrack` for tracking workflow
  - `GPUConfig`, `ProcessingContext` for GPU/CPU execution context
  - `ImageMetadata`, `AnalysisPipeline` for pipeline orchestration
  - Protocol interfaces: `Detector`, `Localizer`, `Tracker`, `FileReader`, `FileWriter`
  - Enums: `DeviceType`, `FileFormat`, `DetectionMethod`, `LocalizationModel`

- Fixed all 4 test failures from job 98937623275:
  1. Added `MAT = "mat"` to `FileFormat` enum
  2. Implemented `to_dataframe()` method on `AnalysisPipeline` with pandas import error handling
  3. Fixed `MockLocalizer` protocol compliance (already structurally compatible)
  4. Fixed `MockTracker` protocol compliance (already structurally compatible)

- All protocols use `@runtime_checkable` decorator for runtime type checking

## Validation evidence

From `nanolocz-gpu/`:

```text
python tools/project_check.py
PASS project scaffold is self-consistent; current card NL-03
PASS 11 required memory/contract files present
PASS 3 tracked task status rows use valid states
PASS canonical package and pyproject.toml are aligned

python -m pytest tests/test_types_nl03.py -v
43 passed in 0.97s

python -m pytest tests/ -v
70 passed, 1 skipped (CuPy not installed) in 1.60s
```

## Next command

```bash
cd nanolocz-gpu
cat STATUS.md SPEC/tasks.md
python tools/project_check.py
```

## Suggested next cards

**Option A (recommended): Start NL-10 — Zarr schema and opener interface**
- Depends: NL-02 ✓, NL-03 ✓
- Define Zarr schema for particle stacks and movies
- Implement opener interface for standardized data access

**Option B: Start NL-50 — PDB ingest to BeadCloud**
- Depends: NL-03 ✓
- Can proceed independently of file format work
- Simulation bridge work unblocked by typed contracts

**Option C: Start NL-11 — .gwy and .h5-jpk openers**
- Depends: NL-10 (requires opener interface first)

## Pending decisions

- Zarr schema design for multi-dimensional AFM data (deferred to NL-10)
- Whether to add optional pandas dependency to pyproject.toml for `to_dataframe()`
- GPU backend selection policy (CuPy vs PyTorch) for future kernel implementations

## Technical notes

- Protocols are structural (duck-typing) via `@runtime_checkable`
- No `__init__` methods required on mock classes implementing protocols
- Only the method signatures defined in Protocol must match
- All dataclasses are frozen where appropriate for immutability guarantees
- Type hints use modern Python syntax (`list[int]`, `tuple[float, float]`)
