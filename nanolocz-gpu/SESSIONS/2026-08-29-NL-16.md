# NL-16 session handoff — detection, statistics, and masks

## Completed

Implemented the NumPy/float64 reference path for NL-16 in `nanolocz/core/detection.py`.

- `fast_peaks2d()` validates 2-D finite input, applies deterministic local-maxima
  detection, optional boolean masks, prominence filtering, and minimum-distance
  suppression.
- Peak rows are stable image order and use `(x, y, height, prominence)`.
- `detect_particles()` now returns the typed `DetectionResult` contract while
  retaining `locs`/`scores`/`angle` compatibility access for old callers.
- Detection results include a boolean output mask and per-detection `area`,
  `volume`, and `eccentricity` arrays.
- Direct and cross-correlation detection both use the typed result.
- `DetectionResult` validates optional mask/statistics shapes and is exported
  from `nanolocz.core`.

## Validation

```text
.venv/bin/python -m pytest tests/test_detection.py -q
9 passed

.venv/bin/python -m pytest -q
165 passed, 1 skipped (CuPy not installed)

.venv/bin/python tools/project_check.py
PASS project scaffold is self-consistent; current card NL-16
```

The project check passed after the status ledger was updated to use its required
`Current card: NL-16` form.

## Scope notes

The checkout contains no MATLAB `Fast_peaks2D.m` golden fixture data, so the
focused tests cover deterministic synthetic parity and contract behavior. A
fixture-backed numerical parity pass should be added when the upstream fixture is
available. GPU acceleration remains a later NL-20/NL-22 concern; `use_gpu` is
accepted but the CPU reference path is intentionally authoritative here.

## Next session

Review and formally reconcile NL-17 acceptance evidence, then continue with the
remaining `.spm`, `.jpk`, `.ibw`, and `.asd` opener cards (NL-12/NL-13).
