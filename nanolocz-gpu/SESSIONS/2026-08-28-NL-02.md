# NL-02 handoff — CPU golden parity harness

Date: 2026-08-28
Card: NL-02
State: done
Commit: `0ab674f feat(port): add CPU golden parity harness`

## Completed

- Added the NL-02 contract in `SPEC/parity.md`.
- Established the fixture hierarchy under `golden/<module>/` for `.npy`, `.npy.sha256`, and provenance files.
- Implemented `nanolocz.parity.fixtures.load_npy_fixture` with required sidecars, SHA-256 validation, filename validation, `allow_pickle=False`, and read-only arrays.
- Implemented centralized CPU and future GPU tolerance policies in `nanolocz.parity.tolerance`.
- Added shape checks, exact integer/boolean comparisons, NaN-aware numerical comparisons, and policy-labelled failures.
- Added seven CPU-only parity tests and a CPU-only GitHub Actions workflow.
- Did not port any NanoLocz analysis logic.

## Validation evidence

From `nanolocz-gpu/`:

```text
python tools/project_check.py
PASS project scaffold is self-consistent; current card NL-02
PASS 9 required memory/contract files present
PASS 3 tracked task status rows use valid states

python -m pytest -q
7 passed in 0.12s
```

## Next command

```bash
cd nanolocz-gpu
cat AGENT.md STATUS.md
python tools/project_check.py
```

## Suggested next card

Start **NL-03 — Typed core contracts**. Implement strict type-checking configuration and serialization tests for `Frame`, `Meta`, `Localizations`, and `ParticleStack`. Do not implement numerical analysis or file openers yet. NL-10 should follow only after NL-02 and NL-03 are complete.

## Pending decisions

- Choose and record the concrete Zarr schema in the NL-03/NL-10 design work.
- Confirm whether metadata serialization should preserve arbitrary `Meta.extras` values or constrain them to a serializable subset.
- Keep MATLAB/Octave golden fixture capture separate from CPU CI until a reference runtime and provenance are available.
