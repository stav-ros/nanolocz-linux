# NL-10 progress handoff — track persistence marker

Date: 2026-08-28
Card: NL-10
State: in_progress

## Completed

- Added `tracks_saved=True` and `n_tracks` attributes whenever `save_tracks()` runs, including `save_tracks([])`.
- Updated `load_tracks()` to distinguish an initialized-but-unsaved tracks group from an explicitly saved empty list.
- Preserved compatibility with legacy stores that contain `track_*` child groups without the marker.

## Validation

From `nanolocz-gpu/`:

```text
.venv/bin/python -m pytest tests/test_io_nl10.py -q
29 passed

.venv/bin/python -m pytest -q
99 passed, 1 skipped (CuPy not installed)

.venv/bin/python tools/project_check.py
PASS project scaffold is self-consistent; current card NL-10
PASS 11 required memory/contract files present
PASS 4 tracked task status rows use valid states
PASS canonical package and pyproject.toml are aligned
```

## Remaining NL-10 work

The track regression is fixed, but NL-10 remains in progress until its complete
acceptance criteria and schema documentation are reviewed. In particular, the
opener currently exposes Zarr while HDF5/TIFF paths remain explicit
`NotImplementedError` placeholders for later opener cards.

## Next step

Review the remaining NL-10 acceptance criteria, then begin NL-11 only after the
Zarr schema/opener contract is formally accepted.
